using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RobLooks : MonoBehaviour
{
    // Start is called before the first frame update
    public Sprite[] sprites;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
